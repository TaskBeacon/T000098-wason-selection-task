# Task Plot Review

## Round 1 — rejected

Evidence match:

- Pass: two representative rows (abstract rule and social contract).
- Pass: fixation -> select cards -> submit set -> blank ITI order.
- Pass: 500 ms and up-to-30-s labels; cards 1 and 4 alone are checked.
- Pass: A/D/4/7 and the four car/fuel card faces are correct and legible.
- Pass: no card backs and no correctness feedback.

Visual quality:

- Fail: the top header band is a dark gray/black gradient rather than clean blank white.
- Fail: the overall canvas uses decorative blue/amber glow and shadow fields instead of a flat white scientific-diagram background.
- Pass: no text overlap, clipping, or malformed Chinese characters.

Revision request:

Keep the same task and overall timeline-collection structure. Fix only these issues:

1. Make the entire canvas a uniform pure white background, including the top 18% header band.
2. Remove all ambient gradients, colored glow fields, vignettes, and large drop shadows; retain only restrained blue/amber accents on labels and thin borders.

Do not add new conditions, phases, stimuli, people, devices, logos, or decorative scenery. Leave a clean blank top header band; the fixed title, `Construct: ...` subtitle, and TaskBeacon logo lockup will be added after generation. Preserve the exact condition and timing labels.

## Round 2 — rejected

- Pass: task evidence, card faces, checkmarks, timings, phase order, and row count remain correct.
- Fail: the canvas still uses a dark gradient with blue/amber glow despite the revision request.
- Fail: the phase-name labels were removed, reducing readability compared with round 1.

Round 3 will edit round 1 directly. Invariant: preserve every row, screen, label, card, arrow, checkmark, and their positions. Change only the canvas/header/background styling to flat white and remove ambient glow/vignette fields.

## Round 3 — accepted

Evidence match:

- Pass: exact two rows and four snapshots per row.
- Pass: fixation -> select -> submit -> blank ITI sequence.
- Pass: rules, A/D/4/7, car/fuel card faces, 1–4 keys, Return, and 500 ms / up-to-30-s timing are correct.
- Pass: only cards 1 and 4 carry checkmarks; no card backs or correctness feedback appear.

Visual quality:

- Pass: uniform white background and blank white top header band.
- Pass: no ambient glow, vignette, decorative scenery, or large shadow fields.
- Pass: all labels are legible without overlap or clipping; screen and card ratios are consistent.
- Pass after post-processing: centered title, centered `Construct:` subtitle, and borderless top-right TaskBeacon lockup are present and do not overlap the timeline.

README embed:

- Pass: `![Task Flow](task_flow.png)` is the first image immediately under `## 2. Task Flow`.
